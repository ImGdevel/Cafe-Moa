import { dbService } from "../FireServer";

const ReserDatabase = dbService.collection("ReservationData");

export async function addReservationDatabase(reservation_time){
    //
}

export async function getReservationDatabase(){
    //const reservation_data = new 
}