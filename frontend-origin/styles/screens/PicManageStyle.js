import { StyleSheet, Dimensions } from "react-native";

const styles = StyleSheet.create({
  button: {
    paddingHorizontal: 8,
    paddingVertical: 6,
    borderRadius: 4,
    backgroundColor: "#ccc",
    alignItems: "center",
    marginHorizontal: "2%",
    marginBottom: 5,
    height: 40,
    minWidth: "45%",
    textAlign: "center",
  },
});

export default styles;
